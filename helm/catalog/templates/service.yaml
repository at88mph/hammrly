apiVersion: v1
kind: Service
metadata:
  name: {{ include "hammrly-catalog.fullname" . }}
  labels:
    {{- include "hammrly-catalog.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hammrly-catalog.selectorLabels" . | nindent 4 }}
