apiVersion: v1
kind: Service
metadata:
  name: {{ include "hammrly-query.fullname" . }}
  labels:
    {{- include "hammrly-query.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hammrly-query.selectorLabels" . | nindent 4 }}
