apiVersion: v1
kind: Service
metadata:
  name: {{ include "hammrly-gateway.fullname" . }}
  labels:
    {{- include "hammrly-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hammrly-gateway.selectorLabels" . | nindent 4 }}
