apiVersion: v1
kind: Service
metadata:
  name: {{ include "hammrly-portal.fullname" . }}
  labels:
    {{- include "hammrly-portal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hammrly-portal.selectorLabels" . | nindent 4 }}
